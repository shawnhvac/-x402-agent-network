#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = require("commander");
const installer_1 = require("./installer");
const logger_1 = require("./utils/logger");
const program = new commander_1.Command();
program
    .name('install-payments-mcp')
    .description('Install payments-mcp for stdio-compatible MCP clients')
    .version('1.0.0');
program
    .command('install')
    .description('Install or update payments-mcp')
    .option('-c, --client <client>', 'MCP client to configure (claude, claude-code, codex, gemini, other)')
    .option('-v, --verbose', 'enable verbose logging')
    .option('-f, --force', 'force reinstallation even if up to date')
    .option('--auto-config', 'automatically configure the MCP client without prompting (for supported clients)')
    .option('--no-auto-config', 'skip automatic configuration prompt')
    .action(async (options) => {
    const logger = new logger_1.Logger(options.verbose);
    const installer = new installer_1.PaymentsMCPInstaller(logger);
    try {
        await installer.install({
            mcpClient: options.client,
            verbose: options.verbose,
            force: options.force,
            autoConfig: options.autoConfig,
        });
    }
    catch (error) {
        process.exit(1);
    }
});
program
    .command('status')
    .description('Check installation status')
    .option('-c, --client <client>', 'show config for specific MCP client (claude, claude-code, codex, gemini, other)')
    .option('-v, --verbose', 'enable verbose logging')
    .action(async (options) => {
    const logger = new logger_1.Logger(options.verbose);
    const installer = new installer_1.PaymentsMCPInstaller(logger);
    try {
        await installer.getStatus(options.client);
    }
    catch (error) {
        process.exit(1);
    }
});
program
    .command('uninstall')
    .description('Remove payments-mcp installation')
    .option('-v, --verbose', 'enable verbose logging')
    .action(async (options) => {
    const logger = new logger_1.Logger(options.verbose);
    const installer = new installer_1.PaymentsMCPInstaller(logger);
    try {
        await installer.uninstall();
    }
    catch (error) {
        process.exit(1);
    }
});
program
    .action(async (options) => {
    const logger = new logger_1.Logger(options.verbose);
    const installer = new installer_1.PaymentsMCPInstaller(logger);
    try {
        await installer.install({
            mcpClient: options.client,
            verbose: options.verbose,
            force: options.force,
            autoConfig: options.autoConfig,
        });
    }
    catch (error) {
        process.exit(1);
    }
})
    .option('-c, --client <client>', 'MCP client to configure (claude, claude-code, codex, gemini, other)')
    .option('-v, --verbose', 'enable verbose logging')
    .option('-f, --force', 'force reinstallation even if up to date')
    .option('--auto-config', 'automatically configure the MCP client without prompting (for supported clients)')
    .option('--no-auto-config', 'skip automatic configuration prompt');
program.configureHelp({
    sortSubcommands: true,
});
program.on('--help', () => {
    console.log('');
    console.log('Examples:');
    console.log('  $ npx @coinbase/payments-mcp');
    console.log('  $ npx @coinbase/payments-mcp install');
    console.log('  $ npx @coinbase/payments-mcp install --force');
    console.log('  $ npx @coinbase/payments-mcp install --client <client>');
    console.log('  $ npx @coinbase/payments-mcp status');
    console.log('  $ npx @coinbase/payments-mcp status --client <client>');
    console.log('  $ npx @coinbase/payments-mcp uninstall');
    console.log('  $ npx @coinbase/payments-mcp --verbose');
    console.log('');
    console.log('Supported MCP clients:');
    console.log('  - claude: Claude Desktop application');
    console.log('  - claude-code: Claude Code');
    console.log('  - codex: Codex CLI');
    console.log('  - gemini: Gemini CLI');
    console.log('  - other: Other stdio-compatible MCP clients');
    console.log('');
    console.log('The default action (no command) is equivalent to "install".');
});
program.parse();
//# sourceMappingURL=cli.js.map