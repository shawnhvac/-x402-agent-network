"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PathUtils = void 0;
const path_1 = __importDefault(require("path"));
const os_1 = __importDefault(require("os"));
class PathUtils {
    static getTempDir() {
        return os_1.default.tmpdir();
    }
    static getInstallationPaths() {
        const homeDir = os_1.default.homedir();
        const installDir = path_1.default.join(homeDir, '.payments-mcp');
        const packageJsonPath = path_1.default.join(installDir, 'package.json');
        const electronDir = path_1.default.join(installDir, 'node_modules', 'electron');
        const installerScript = path_1.default.join(electronDir, 'install.js');
        return {
            tempDir: this.getTempDir(), // Keep temp dir for temporary files during download
            installDir,
            packageJsonPath,
            electronDir,
            installerScript,
        };
    }
    static getNodeExecutable() {
        return process.execPath;
    }
    static getNpmExecutable() {
        return process.platform === 'win32' ? 'npm.cmd' : 'npm';
    }
}
exports.PathUtils = PathUtils;
//# sourceMappingURL=pathUtils.js.map