import { AxiosResponse } from 'axios';
import { HttpRetryOptions } from '../types';
import { Logger } from './logger';
export declare class HttpUtils {
    private logger;
    private defaultRetryOptions;
    constructor(logger: Logger);
    private getHttpsAgent;
    private sleep;
    private executeWithRetry;
    get<T = unknown>(url: string, options?: Partial<HttpRetryOptions>): Promise<AxiosResponse<T>>;
    downloadFile(url: string, filePath: string, onProgress?: (progress: {
        transferred: number;
        total: number;
        percent: number;
    }) => void, options?: Partial<HttpRetryOptions>): Promise<void>;
    head(url: string, options?: Partial<HttpRetryOptions>): Promise<AxiosResponse>;
    getErrorMessage(error: unknown): string;
}
//# sourceMappingURL=httpUtils.d.ts.map