import { InstallOptions, MCPClient } from './types';
import { Logger } from './utils/logger';
export declare class PaymentsMCPInstaller {
    private logger;
    private versionService;
    private downloadService;
    private installService;
    private configService;
    private processUtils;
    constructor(logger: Logger);
    install(options?: InstallOptions): Promise<void>;
    private promptForMCPClient;
    private performPreflightChecks;
    private handleRunningProcess;
    private checkVersions;
    private performInstallation;
    private postInstallSetup;
    private handleAutoConfiguration;
    private displayCurrentConfig;
    private handleInstallationFailure;
    getStatus(mcpClient?: MCPClient): Promise<void>;
    uninstall(): Promise<void>;
}
//# sourceMappingURL=installer.d.ts.map