import { VersionInfo, InstallationStatus } from '../types';
import { Logger } from '../utils/logger';
export declare class VersionService {
    private httpUtils;
    private fileUtils;
    private logger;
    private readonly versionApiUrl;
    constructor(logger: Logger);
    getLocalVersion(): Promise<string | null>;
    getRemoteVersion(): Promise<string>;
    compareVersions(localVersion: string | null, remoteVersion: string): Promise<boolean>;
    getVersionInfo(): Promise<VersionInfo>;
    getInstallationStatus(): Promise<InstallationStatus>;
    validateVersion(version: string): boolean;
    isNewerVersion(version1: string, version2: string): boolean;
    formatVersionInfo(versionInfo: VersionInfo): string;
}
//# sourceMappingURL=versionService.d.ts.map