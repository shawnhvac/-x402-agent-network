import { Logger } from '../utils/logger';
export declare class DownloadService {
    private httpUtils;
    private fileUtils;
    private logger;
    constructor(logger: Logger);
    private getDownloadUrl;
    downloadAndExtract(targetPath: string, version: string): Promise<void>;
    private downloadWithProgress;
    private extractZip;
    private extractEntry;
    private sanitizeEntryPath;
    private isUnsafePath;
    validateDownload(filePath: string): Promise<boolean>;
    getDownloadUrlAsync(version: string): Promise<string>;
    checkDownloadAvailability(version: string): Promise<boolean>;
}
//# sourceMappingURL=downloadService.d.ts.map