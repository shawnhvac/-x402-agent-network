import { Logger } from '../utils/logger';
export declare class InstallService {
    private fileUtils;
    private logger;
    constructor(logger: Logger);
    runNpmInstall(projectPath: string): Promise<void>;
    runElectronInstaller(projectPath: string): Promise<void>;
    verifyInstallation(projectPath: string): Promise<boolean>;
    getInstalledPackageInfo(projectPath: string): Promise<{
        name: string;
        version: string;
    } | null>;
    private executeCommand;
    checkNpmAvailability(): Promise<boolean>;
    checkNodeAvailability(): Promise<boolean>;
    cleanupFailedInstallation(projectPath: string): Promise<void>;
}
//# sourceMappingURL=installService.d.ts.map