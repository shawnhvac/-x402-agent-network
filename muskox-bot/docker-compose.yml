version: '3.8'

services:
  muskox-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
    networks:
      - muskox-network

networks:
  muskox-network:
    driver: bridge
